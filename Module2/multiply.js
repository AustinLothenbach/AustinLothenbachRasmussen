function multiply() {
	var x = parseInt(document.getElementById("c").value);
	var y = parseInt(document.getElementById("d").value);

	var product = (x) * (y);
	alert(x + " * " + y + " = " + product);
}